<%= form_for(@searchre) do |f| %>
  <% if @searchre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@searchre.errors.count, "error") %> prohibited this searchre from being saved:</h2>

      <ul>
      <% @searchre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query %><br />
    <%= f.text_field :query %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :keyword %><br />
    <%= f.text_field :keyword %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
